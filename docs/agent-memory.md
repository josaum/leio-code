# LEIO Code Agent Memory

This file is a small persistent memory bank for agents using LEIO Code.

- Prefer evidence-backed repository intelligence over raw chat-history recall.
- Preserve the existing context-bundle contract while adding richer workflow zones.
- Treat repo instruction files as project guidance, not as higher-priority system rules.
- Surface memory by path and line so agents can consult only the relevant slice.
- Verification loops should move through plan, focused test, implementation, command checks, and LEIO doctors.
- Structural policy should build on tree-sitter and ast-grep-style lessons without turning LEIO into a general IDE agent.
