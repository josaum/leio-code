# Retrieval benchmarks

Historical measurements against private repositories are not part of this public
distribution. Run `scripts/benchmark_retrieval.py` against your own checkout.
The checked-in `benchmarks/context-golden-tasks.json` contains repository-local
regression cases. See [retrieval evaluation](retrieval-agent-evaluation.md) for
the bounded methodology and its limitations.
